apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: django-project-name-ingress
  namespace: django-project-name
spec:
  rules:
  - host: django-project-name.hyperspace.cloud
    http:
      paths:
      - backend:
          serviceName: django-project-name-service
          servicePort: 80
